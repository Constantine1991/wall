#include "typecomponent.h"
#include "ui_typecomponent.h"
/*
TypeComponent::TypeComponent(QWidget *parent) :
    QDialog(parent),
    ui(new Ui::TypeComponent)
{
    ui->setupUi(this);
}

TypeComponent::~TypeComponent()
{
    delete ui;
}

void TypeComponent::on_pushButton_clicked()
{
    int width=ui->lineEdit->text().toInt();
    TYPEITEM type;
    if(ui->radioButton->isChecked())
        type=ITEM_WICKET;
    if(ui->radioButton_2->isChecked())
        type=ITEM_GATE_A;
    if(ui->radioButton_3->isChecked())
        type=ITEM_GATE_B;
    emit this->component(type,width);
    this->close();
}

void TypeComponent::on_pushButton_2_clicked()
{
    this->close();
}
*/
